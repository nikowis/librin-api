<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                      http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.8.xsd">

    <changeSet id="changeset2020-11-23-user-ratings.xml" author="nikowis">
        <sql>
            CREATE TABLE "rating" (
                id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY
                , value SMALLINT NOT NULL
                , user_id BIGINT NOT NULL
                , author_id BIGINT NOT NULL
                , offer_id BIGINT NOT NULL
                , description VARCHAR(512)
                , created_at TIMESTAMP NOT NULL
                , FOREIGN KEY (user_id) REFERENCES "user" (id)
                , FOREIGN KEY (offer_id) REFERENCES "offer" (id)
                , FOREIGN KEY (author_id) REFERENCES "user" (id)
            );
            CREATE INDEX USER_ID_CREATED_AT_RATING ON "rating" ("user_id", "created_at");

            ALTER TABLE "user" ADD COLUMN "avg_rating" real DEFAULT 0;
            ALTER TABLE "user" ADD COLUMN "rating_count" INT DEFAULT 0;
        </sql>
    </changeSet>
</databaseChangeLog>



